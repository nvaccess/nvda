Import([
	'env',
	'remoteLib',
])

vbufBaseStaticLib=env.StaticLibrary(
	target="vbufBase_static",
	source=[
		"storage.cpp",
		"utils.cpp",
		"backend.cpp",
		remoteLib[1] if 'symbols' not in env['nvdaHelperDebugFlags'] else remoteLib[2],
	],
)

Return('vbufBaseStaticLib')
