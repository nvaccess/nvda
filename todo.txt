*Need to find a type library (.tlb file) or dll that contains the IServiceProvider com interface. It doesn't seem to come with Windows by default. IServiceProvider is needed for mozilla to retrieve its HTML dom interfaces.
*Need to Add support for frames in internet explorer.
*Need to Add events in internet explorer that can update parts of the page due to things such as javascript.
*Need to make the edit message window in outlook express accessible. It is an internet Explorer_server, but doesn't work for some reason.
